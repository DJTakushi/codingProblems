#include <vector>
#include <iostream>
#include <string>
#define VERSION_MAJOR @lp_VERSION_MAJOR@
#define VERSION_MINOR @lp_VERSION_MINOR@
#define PROJECT_NAME "@PROJECT_NAME@"
#define PROJECT_DESCRIPTION "@PROJECT_DESCRIPTION@"
#define PROJECT_HOMEPAGE_URL "@PROJECT_HOMEPAGE_URL@"


using namespace std;

class Solution {
public:
    vector<string> getDescription();

    /// gets biggest palindrome starting at index `i` 
    /// of string `s` with starting indexes `left` and `right`
    string getPalindromeAtRoot(string s, int i, size_t left, size_t right);
    string longestPalindrome(string s);
};
